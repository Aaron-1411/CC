# SPA fallback for client-side routing. Pages Functions (/api/*) are matched
# first via the auto-generated _routes.json, so this rewrite never swallows the
# API. Everything else serves the React app.
/*    /index.html   200
